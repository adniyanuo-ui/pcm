# 开发验收与小范围试用

## 分工与发布门槛

开发、基础测试、浏览器回归和问题修复由开发代理完成。用户只在开发环境进行一次整体业务验收；若有问题，反馈后由代理修复并回归相关环节。代码再次改变时，需要用户确认受影响的业务结果。

仅当完整流程通过且用户明确说“发布”，才能推送香港生产 `main`。届时代理执行发布脚本，用户无需手工操作 Git。生产部署成功后先验证部署版本与虚构病例冒烟测试，再让 1～3 位已授权年轻医生试用。发布脚本不创建医生账号、不发送邀请。

## 本次实现

- 独立的医生归属患者、就诊和不可覆盖的历史快照；账号只能访问自己的就诊。
- 真实工作台创建患者、复诊，并以问诊记录、辨证论治、选方遣药、病历确认四页完成闭环。问诊校对稿下接望切补充，不另调模型重复整理。
- 原始识别依据与医师校对稿分别保存；录音经同意后私有保留30天，按症状定位回放，到期禁止访问并自动清理。
- 模型整理录音症状与就诊信息、起草辨证证据摘要；新版问诊确认不调用模型改写医师校对稿。不使用旧问卷、历史五星病例或内部推理文本。
- 医生端不显示型号和供应商；后台保存请求型号与实际响应型号，管理员可显式配置备用，失败不静默切换。
- 病历按已确认字段逐项汇总，未提供的检查和诊断标为待填；最终由医生补充和确认。
- 修改患者信息、转写、四诊、辨证或处方会使相关下游失效；并发修改返回版本冲突，过期生成不能覆盖新资料。
- 每步有实际保存时间、未保存提示，支持刷新及重新登录恢复。各页只保留主确认动作并同时保存当前修改，不显示重复的独立保存按钮。空白、过长或仍含“剂量待核”的处方不能确认；不再强制分栏填写，正文的药味、剂量和用法完整性由医师审核后明确确认。老版结构化处方仍可读取，后台修改历史不删除。
- 基础方列表和医生编辑框同页，最多五方分次追加，追加不覆盖医生修改。组成可靠可分时增加顿号；古代重量仅按医生填写并确认的换算依据转换为克，数量、容量和缺失剂量不推测。

## 自动化验证

新加坡 `.env.local` 已填写并修正凭据。2026-09-05 真实 NLS 连接与 DeepSeek 浏览器闭环已通过，原 AccessKey 校验问题已解除。以后更新配置可在服务器终端调用现有不回显工具（无需粘贴进聊天）：

```bash
./pcm-master_hyd/.venv/bin/python deploy/light-server/configure-secrets.py pcm-master_hyd/.env.local
```

配置后重启开发服务。DeepSeek 为主用，Qwen 可留作显式备用；录音需阿里云三项配置。

```bash
PCM_ENV=test ./pcm-master_hyd/.venv/bin/python pcm-master_hyd/manage.py test llm user speech pcm llm_utils.rag.tests
PCM_ENV=test ./pcm-master_hyd/.venv/bin/python pcm-master_hyd/manage.py makemigrations --check --dry-run
npm --prefix pcm_doctor_web test
npm --prefix pcm_doctor_web run build
git diff --check
```

真实 Chrome、独立临时 Django 数据库、随机临时账号、真实 RAG 索引测试：

```bash
PCM_ENV=test PCM_BROWSER_TEST=1 ./pcm-master_hyd/.venv/bin/python pcm-master_hyd/manage.py test llm.test_workbench_browser
```

上面的浏览器测试用夹具替代语音识别文本和模型返回；录音采集、分片上传、私有回放、登录、数据库、网页及RAG均走真实软件链路。要同时验证真实模型，先在不进入 Git 的 `pcm-master_hyd/.env.local` 配置开发专用密钥，再运行：

```bash
set -a
source pcm-master_hyd/.env.local
set +a
PCM_ENV=test PCM_BROWSER_TEST=1 PCM_REAL_MODEL_TEST=1 ./pcm-master_hyd/.venv/bin/python pcm-master_hyd/manage.py test llm.test_workbench_browser
```

测试使用本机 5176 和 9228 端口，结束后关闭测试服务与 Chrome。默认通过识别夹具提供虚构诊疗文本，覆盖实时症状、结束自动整理及录音回放；实际麦克风与真人语音识别质量应在用户验收中覆盖。

额外设置 `PCM_REAL_SPEECH_TEST=1`，可加入真实 NLS 临时令牌、WebSocket 和 Chrome 模拟麦克风的开始/暂停/继续/停止测试；这种模式随后手工填写虚构就诊信息，不把模拟音源作为真人语音准确率验证。是否使用真实整理模型仍由 `PCM_REAL_MODEL_TEST` 单独控制。

2026-09-06反馈修订版验证结果：后端49项（48通过、1项默认跳过的浏览器测试）、前端12项、生产构建及迁移一致性通过。独立浏览器测试三种模式分别通过：识别/模型夹具28.923秒、识别夹具与真实Flash/Pro 93.504秒、真实NLS与模型夹具26.993秒。真人录音准确率尚需用户复核，不能以这些自动化结果替代。

同日第二轮极简修订后：后端57项（56通过、1项默认跳过）、前端14项、构建及迁移一致性通过；夹具浏览器37.334秒、真实模型浏览器75.720秒、真实NLS与模型夹具49.188秒通过。浏览器新增左右录音布局、四步合并、正文大字号、窄屏溢出、处方正文和问诊修改后的下游失效回归。真实模型样例中，最终语音整理界面耗时4.38秒；仍用Pro，无需切换Flash，但不保证任意长录音或网络状况下均在一分钟内完成。

同日第三轮修订后：后端67项（66通过、1项默认跳过）、前端16项、生产构建、Django检查及迁移一致性通过；最终夹具浏览器35.478秒、真实DeepSeek与RAG浏览器106.113秒、真实NLS与模型夹具浏览器29.213秒通过。覆盖一次点击结束录音、三段辨证、同页选择三方、保留医生修改、无自动滚动、剂量待核门禁、所有方剂出处进入病历及去掉重复保存按钮。真实模型流程中的结束整理显示耗时2.88秒；这仍是虚构短样例，不构成任意录音的一分钟服务承诺。

语音异常回归已覆盖：凭据请求失败释放麦克风、不支持录音时显示错误、握手前断线结束等待、停止后清理定时器、开始后服务报错释放设备。

## 用户一次整体验收

在开发网页用虚构病例：创建就诊 → 在左侧录音机确认录音同意 → 录音时查看右侧症状关键词 → 暂停、继续 → 一次点击“结束录音，自动整理” → 回放症状对应原音并校对大字号问诊稿 → 在同页补充望切并确认最终问诊 → 按三段修改确认辨证论治 → 查看完整基础方及辞典页码 → 同页选择两至三个基础方带入 → 修改底部处方正文 → 再追加一方并确认原修改未丢失 → 核对剂量、用法后确认 → 编辑确认病历 → 刷新并重新登录恢复 → 导出病历。再返回修改一处问诊或望切，确认旧处方和病历失效。全流程不应出现重复的“保存问诊/保存修改/保存处方/保存草稿”按钮。

录音与中文识别质量必须使用实际麦克风复核；模拟音源/识别夹具仅验证软件链路，不能作为识别准确率结论。

## 30天录音生命周期

- 私有目录默认 `pcm-master_hyd/var/private_audio/`，不提供静态URL；回放需携带所属医生的认证信息，音频响应禁止缓存。
- 每段录音自开始起30天到期；到期立即拒绝新的回放请求。开发启动脚本和待发布Compose配置包含每60秒执行一次的清理进程，正常运行时物理删除延迟不超过一个检查周期。
- 开发启动脚本会运行迁移，并随开发服务启停清理进程；部署配置仅在批准发布后生效，当前不操作香港。
- 上传采用10秒WAV分片、顺序与格式校验、重复上传幂等保护。网络失败时未上传内容暂留当前页面，明确提示不要刷新；页面关闭或崩溃后未上传的最后片段无法恢复，不宣称零丢失。
- 正常录音不再显示列表；检测到上次录音中断时显示收尾提示，仅保留服务器已收到的部分；该动作不恢复缺失音频或延长保留期。
- 音频不得进入长期备份/服务器永久快照；备份策略必须排除私有音频目录，否则运行目录清理不能保证备份副本同步消失。首轮仍仅使用虚构/授权脱敏录音。
- 文字病历、原始识别依据、医师修改历史与音频分开保留；音频清理不删除这些资料。

## 一键发布入口

```bash
bash scripts/release-pilot.sh --approved-commit <用户验收通过的完整提交SHA>
```

脚本检查 `develop`、工作区干净、验收 SHA、测试和远程分支祖先关系，然后原子推送 `develop` 与 `main`，触发既有香港自动部署。没有强制推送，不跳过验收。推送成功不等于上线完成，代理需继续验证生产部署结果。生产数据库变更前需有可用备份；当前脚本不负责远程数据库备份。
