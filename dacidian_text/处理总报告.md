# 《中医方剂大辞典》第 1—11 册转换总报告

## 已完成成果

第 1—10 册正编和第 11 册附编已全部完成一次性 OCR、结构清洗、可追溯纠错和压缩归档。原 PDF、渲染图片和逐页展开文件不进入 Git；可复现脚本、纠错依据、结构化文本、疑点清单、质量报告以及压缩后的原始 OCR 进入版本管理。

正编结果：

- 方号 `00001—96592` 连续；
- 共 96592 条记录；
- 各册均为零缺号、零重号、零倒序；
- 可进入后续 RAG：96381 条；
- 因源扫描缺页排除 RAG：211 条，其中完全缺失 187 条、部分缺失 24 条；
- 源 PDF 全白正文页：25 页；
- 原始正文 OCR 行：949057；
- 加权平均置信度：0.980519；
- 低于 0.70 的正文行：3508；
- 经页码和扫描原页确认的标题修正：239；
- 通过顺序和目录修复的损坏方号：99；
- 通过中型正文 OCR 与独立目录一致后修正的方名：711；
- 尚待人工核对的标题三方争议：772。

附编结果：

- 950 页全部识别成功；
- 方名索引直接定位 96212 条，覆盖率 99.61%；
- 用正编补齐后的逻辑方名索引为 96592 条；
- 病证索引整理出 2311 个病证词、83052 条病证—方号关系；
- 度量衡与主要引用书目已保真保存并生成视觉行数据。

## 数据入口

- `corpus/fangji_entries.jsonl`：正编全量主语料；
- `corpus/rag_exclusions.jsonl`：不得进入 RAG 的缺页记录；
- `corpus/review_index.jsonl`：所有卷册质量标记的汇总索引；
- `corpus/unresolved_header_audit.jsonl`：方名独立复核后仍未解决的疑点；
- `corpus/quality_summary.json`：正编全书统计和文件哈希；
- `volume_11/`：附编方名索引、病证索引、度量衡和引用书目。

## 对后续 RAG 的建议

1. 首轮只索引 `fangji_entries.jsonl` 中 `record_status` 缺省或为 `complete` 的 96381 条记录；
2. 每个检索片段保留方号、册号、PDF 页和书内页，回答必须能回溯原文；
3. 病证索引作为扩展召回通道，不直接替代四诊合参、病机、治法和方剂正文证据；
4. 对 772 条标题争议和低置信度正文按实际命中频率逐步人工复核，不阻塞最小 RAG 原型；
5. 找到其他完整版本后，优先补齐 25 个全白源页对应的 211 条记录，再解除 RAG 排除标记。
