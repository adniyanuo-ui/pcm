# 《中医方剂大辞典》第 11 册处理报告

## 结论

第 11 册 950 页纯扫描 PDF 已完成一次性 OCR 和分段结构化，页面识别失败数为 0。昂贵的逐页识别结果已压缩保存，可在不重新 OCR 的情况下反复调整解析规则。

第 11 册是附编，不含新的方剂正文，结构如下：

- PDF 1—11 页：封面、总目等前置页；
- PDF 12—653 页：正词目方名索引；
- PDF 654—757 页：副词目方名索引；
- PDF 758—929 页：主治病证索引；
- PDF 930—933 页：古今度量衡对照；
- PDF 934—950 页：主要引用书目。

PDF 第 757 页为空白分隔页，位于副词目结束和主治病证索引开始之间，不作为正文缺页处理。

## 识别和结构化结果

- 原始 OCR 行：293825；
- 原始行平均置信度：0.993680；
- 低于 0.70 的行：757；
- 方名索引 OCR 可靠定位：96212/96592（99.61%）；
- 重复误号自动重定向：375；
- 未能从第 11 册 OCR 可靠定位：380；
- 仍待复核的重复方号：212；
- 方名与正编低相似疑点：2170；
- 逻辑完整方名索引：96592，其中 380 条由第 1—10 册正编补入并显式标注；
- 病证词：2311；
- 病证—方号关系：83052；
- 病证索引涉及的唯一方号：71115；
- 度量衡表视觉行：100；
- 引用书目视觉行：756。

## 主要输出

- `volume_11_raw_pages.jsonl.gz`：可恢复坐标和置信度的原始 OCR；
- `volume_11_formula_index.jsonl`：第 11 册 OCR 可定位的方名索引；
- `volume_11_formula_index_complete.jsonl`：与正编合并后的 96592 条逻辑完整索引；
- `volume_11_formula_index_needs_review.jsonl`：缺号、重复和名称差异疑点；
- `volume_11_syndrome_index.jsonl`：病证到方号的结构化关系；
- `volume_11_measures.txt`、`volume_11_measure_rows.jsonl`：度量衡保真文本和视觉行；
- `volume_11_bibliography.txt`、`volume_11_bibliography_rows.jsonl`：引用书目保真文本和视觉行；
- `volume_11_quality.json`、`manifest.json`：质量统计、来源哈希和输出哈希。

## 使用边界

方名索引是正编的检索附录，不能覆盖第 1—10 册正文事实。病证索引适合做 RAG 的召回扩展或候选方入口，但当前只完成机器结构校验，尚未由中医专家抽样确认病证词分段和方号归属，接入临床工作台前应做抽样复核。度量衡和引用书目保留了逐页文本与坐标证据，复杂表格不应只依赖线性文本顺序。
