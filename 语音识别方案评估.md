# 语音识别方案评估（2026-09-05）

## 当前决定

保留已联通的阿里NLS作为当前ASR，不把“后续文字整理”冒充识别准确率提升。新增可配置的中医热词表入口，但未创建/配置词表时不声称已启用。录音中Flash提取症状、结束后Pro整理全文，原音与句子时间位置用于医生核对。

当前新加坡实例实测总内存约3.4GiB、可用约2.4GiB，还需运行数据库、RAG和Web；本轮没有下载大型模型，也没有更换生产识别服务。必须在资源与同批录音验证通过后才决定替换。

## 开源候选与依据

- [FunASR官方仓库](https://github.com/modelscope/FunASR)：包含流式识别、VAD、标点及多种部署选项。流式场景优先评估Paraformer路线，比较首个结果延迟、结束收尾、热词和中文医患对话错误。
- [SenseVoice官方仓库](https://github.com/QwenAudio/SenseVoice)：可用于中文等语言识别，列为CPU离线/分段复核候选；不能把离线推理快等同于端到端流式链路成熟，也没有本项目临床录音胜出证据。
- [FunASR部署矩阵](https://github.com/modelscope/FunASR/blob/main/docs/deployment_matrix.md)：不同模型/运行时适合的CPU、GPU、流式与热词场景不同，应先确定运行方式与实际延迟，不能仅凭模型名直接替换。
- [阿里NLS热词设置](https://help.aliyun.com/zh/isi/developer-reference/use-an-sdk-to-specify-the-id-of-a-business-specific-hotword-vocabulary)：支持 `vocabulary_id`。本项目通过后端可选 `ALIYUN_NLS_VOCABULARY_ID` 下发词表ID，长期访问密钥仍留在后端。

## 替换门槛

用同一组虚构病例录音覆盖安静、背景噪声、医患交替、口音、否定、时间、药名与剂量。医生逐句确认参考文本后，分别测字符错误率、临床关键事实错误、首个症状延迟、最终整理延迟、录音对齐、内存和并发占用。

尤其检查“没有发热”变为“发热”、医生询问变成患者确有症状、药名被猜测、剂量被自动改写；这些错误不能由总体错字率掩盖。只有关键事实更可靠且部署资源满足时才在develop显式切换，保留旧方案可回退，再经过人工确认发布。

## 整理与识别的区别

DeepSeek文本整理看不到原始音频，不能从声音重新识别错字。提示要求只做有上下文依据的规范化，保留否定、病程、主语及疑点；每个症状必须引用提供的句子ID，后端拒绝虚构ID。该检查保证引用存在，不等于语义正确，医生仍需按原音复核。

实时关键词及最终全文整理请求按[DeepSeek官方参数](https://api-docs.deepseek.com/guides/thinking_mode/)关闭额外推理以减少延迟；这不改变最终全文整理使用Pro，也不向医生展示内部思维链。2026-09-06第二次反馈后，另取消医师校对稿到四诊的重复模型改写，并对同批录音重复结束复用结果，避免覆盖校对稿。

优化后两组165字、990字虚构对话分别实测3.57秒、6.90秒；真实模型浏览器样例从结束按钮到整理稿显示为4.38秒。这些是软件验证样例，不是所有录音长度、网络条件下的时延保证，也不是识别质量证据。模型网络等待设为45秒、无自动重试；失败保留已保存资料并提示重试，不静默换模型。可用 `scripts/benchmark-voice-final.py` 在显式加载开发凭据后复测，该脚本只构造虚构文本，不读取患者数据库。
