# PCM 云端开发交接

更新时间：2026-09-06

## 当前环境

- 香港生产实例：仅自动拉取 GitHub `main`，公开地址为 `https://www.doctorczc.com`。
- 新加坡开发实例：工作目录 `/srv/pcm-workspace/pcm`，默认分支 `develop`。
- 生产数据库、生产 API 密钥与开发环境隔离。

## 已完成能力

- 医生账号登录及公开注册关闭；
- HTTPS与GitHub自动部署；
- 浏览器真实麦克风采集及阿里云NLS实时转写链路；
- 《中医方剂大辞典》正编 96,381 条可检索方剂及第11册病证索引；
- 带辞典册数、页码和原文依据的RAG候选方检索；
- DeepSeek V4 Pro真实流式调用已验证，Qwen3.7 Plus保留为显式备用；
- 极简医生工作台四步界面（问诊与四诊已合并），旧版五步就诊记录兼容读取。

## 下一阶段唯一优先事项

完成本次人工反馈修订版的开发环境验收，再经用户明确确认发布，供1～3位年轻医生试用。

就诊持久化到病历确认的闭环已经完成，用户已完成两轮整体试用；最新修订按第二轮反馈收拢极简界面，重点待复核左录音机右症状、合并问诊与望切补充、紧凑辨证草稿、选方后直接编辑处方正文及实际录音整理耗时。开发仍仅用虚构或授权脱敏病例，不复制生产患者数据。

验收通过前不合并或推送 `main`。第四模块（医生风格）、第五模块（推荐追问）和专科问卷均不开发。

## 新加坡闭环开发进度（2026-09-05）

- `develop` 工作区已实现独立患者/就诊/版本历史接口及真实医生工作台，已替换真实模式的硬编码病例、病机、处方和保存提示。
- 已实现账号归属、顺序确认、上游修改后的下游失效、并发版本冲突、医师修改记录、病历导出以及刷新/重新登录恢复。
- 病历草稿采用已确认字段确定性汇总，缺失检查和疾病诊断标为待填；不让模型补造临床事实。方剂及引用来自真实 RAG，处方由医生对照原文填写。
- 新增迁移 `llm.0009` 已应用到新加坡独立 `var/dev.sqlite3`；香港未更改。
- Chrome 已用虚构病例通过登录到病历保存、刷新和重新登录恢复；此项使用真实 API/数据库/RAG，仅模型输出替换为测试夹具。
- 用户已在新加坡配置五项开发凭据。真实 DeepSeek → 辨证 → RAG → 处方 → 病历确认 → 刷新/重新登录恢复的 Chrome 测试已通过，使用独立测试数据库与虚构病例。
- 用户修正开发凭据后，真实 NLS 临时令牌、Chrome 模拟麦克风采集、WebSocket 开始/暂停/继续/停止均通过；随后真实 DeepSeek、RAG、病历确认、刷新与重新登录恢复的完整浏览器测试通过（79.695 秒，无浏览器异常）。原 `InvalidAccessKeyId.NotFound` 阻塞已解除。
- Qwen3.7 Plus 显式备用调用长时间未返回，尚未通过真实联调；继续使用已验证的 DeepSeek 主路径，不静默切换。按阿里云文档尝试关闭思考后仍未及时完成，未保留该试验参数。
- 已修复语音凭据失败时的设备释放、握手前断线等待、停止定时器清理及连接建立后错误处理；前端共 9 项回归通过。
- 下一步由用户在新加坡开发环境使用虚构病例及实际麦克风做一次整体业务验收。自动化语音测试使用模拟音源，不代表真人语音识别质量已验证；Qwen 仍未通过备用联调，主流程使用已验证的 DeepSeek。用户明确发布后才进入 `main` 并安排 1～3 位医生试用。
- 一次验收、自动化命令与一键发布入口见 `开发验收与发布.md`。`scripts/release-pilot.sh` 尚未执行，没有生产推送。

## 人工反馈修订版（2026-09-06）

- 已完整阅读开发共识、交接文档及 `pcm_product_text/` 中五篇设计资料的提取全文；本工作区未找到对应Word原件，不声称已直接读取原件。
- 录音中提取症状关键词，不展示逐字对话；结束后自动整理就诊信息，医生可以修改。实时提取使用Flash，最终整理使用Pro，前台不展示供应商、型号或内部思维链。
- 新增私有PCM录音分片、录音同意、症状到句子及时间位置的引用、认证回放、30天到期拒绝访问与清理；保留文字病历及修改历史。网络中断可重试，页面崩溃前尚未上传的片段无法恢复，界面明确提示。
- 辨证按资料层、定性层、病机与治法捆绑、证型归纳四层展示，证据与不确定性置后默认折叠；推荐追问不生成、不展示。
- 等待时展示醒目的当前操作、动画和等待时间，不伪造完成百分比或模型推理过程。
- 基础方展示完整辞典字段与全部引用页码；采用后直接打开医师加减窗口。`usage` 明确为“煎服方法”，校验改为中文。古代剂量不自动换算，处方仍由医师填写确认。
- 已评估FunASR、SenseVoice等开源候选，但无本项目同批真人录音对比证据，本轮不替换ASR。新增可选NLS热词表配置入口，未配置时不视为已启用；详见 `语音识别方案评估.md`。
- 自动化回归：后端49项（48通过，1项浏览器测试按默认开关跳过），前端12项通过，生产构建、迁移一致性和Compose配置校验通过。
- 独立浏览器验证：语音/模型夹具全流程通过（28.923秒）；语音夹具与真实Flash/Pro全流程通过（93.504秒）；真实NLS令牌、WebSocket、模拟麦克风与模型夹具全流程通过（26.993秒）。各自使用独立测试数据库与虚构病例；模拟音源不验证真人识别质量。
- `llm.0010` 仅应用到新加坡独立开发数据库，迁移前已在忽略目录保存开发数据库备份。开发服务已重启，Web/API仅监听本机5174/8000，健康检查通过；录音清理进程已确认运行。
- 本次修改仍在 `develop` 工作区，未提交、未推送、未发布；香港生产、生产数据库和生产密钥未操作。待用户验收受影响流程并明确发布后，再执行发布门槛。

## 第二轮反馈：极简四步工作台（2026-09-06）

- 恢复原有左侧深绿色圆形录音机，右侧保留症状关键词与原音定位回放；就诊整理稿字号增至20px。移除正常录音列表和修改历史入口，但不删除后台依据和历史，仅中断录音显示必要的收尾提示。
- 前台四步为问诊记录、辨证治法、选方遣药、病历确认。问诊与四诊合并一页，校对后的文本直接保存为最终问诊，下接望切补充，闻诊按需展开；新增原子保存/确认接口，不再进行重复的四诊模型改写。旧版五步确认状态映射为四步显示。
- 辨证去掉四层的大标题和说明，同一草稿区域保留七项可编辑内容，证据折叠。处方改为单一正文编辑页，选方时直接带入辞典组成和用法，不自动推测、拆药或换算剂量；兼容旧结构化处方全部信息。空白不能保存，确认处方仍须显式医师审核。
- 最终语音整理仍使用Pro，关闭额外推理、网络等待45秒且不自动重试；同批录音重复结束复用结果，不覆盖医师校对稿。165字、990字虚构对话实测3.57秒和6.90秒；浏览器样例结束到整理显示4.38秒，暂不需切换Flash。样例耗时不代表所有实际录音的时限保证。
- 后端57项（56通过、1项默认跳过的浏览器测试）、前端14项、构建、Django检查与迁移一致性通过。夹具浏览器全流程37.334秒、真实模型浏览器75.720秒、真实NLS与模型夹具浏览器49.188秒均通过；包含录音、上传、症状回放、四步确认、自由文本处方、刷新/重登与上游修改后重新生成。真实NLS仍使用模拟麦克风，不用于评价真人识别率。
- 已检查桌面与390px窄屏截图，录音机左右布局及处方带入原文正常，窄屏无横向溢出。截图在开发实例临时目录，均为虚构病例。
- 无新增数据库迁移，独立开发服务已重启。所有改动仍在新加坡 `develop` 工作区；未提交、未推送、未执行发布脚本，香港生产未操作。下一步仅需用户确认此次受影响流程，明确发布后再进入生产门槛。

## 第三轮反馈：一键录音、三段辨证与同页多方（2026-09-06）

- 结束录音改为单次点击完成录音停止、尾段上传、会话关闭和自动整理；新增串行上传队列的尾段竞争及失败重试测试，前台统一为“问诊信息整理稿”。
- 辨证论治改为三段：病因/病位/病性/病势，病机/证型，治则/治法；内容为主，支持证据与不确定性仍默认折叠。
- 候选基础方与医生编辑框在同一页面，支持最多五方分次追加；不跳页、不自动滚动，追加不覆盖医生已修改正文。病历保留所有采用方的辞典册数与页码。
- 可靠识别的辞典组成增加顿号，原文已有克/毫克剂量统一显示为克。缺失剂量标为待核并阻止误确认；古代重量只有医生填写完整换算单位和依据后才换算，换算依据进入病历审计，数量及容量不转为克。
- 移除问诊、辨证、处方和病历页重复的独立保存按钮，主确认动作负责保存；必要的错误重试、未保存保护和导出保留。
- 当前自动回归：后端67项（66通过、1项默认跳过）、前端16项与生产构建通过；最终夹具Chrome全流程35.478秒，真实DeepSeek与RAG全流程106.113秒（真实结束整理显示2.88秒），真实NLS与模型夹具29.213秒，均使用隔离测试库及虚构数据。实际真人语音准确率仍只由人工验收判断。
- 修改只固定在新加坡 `develop` 本地提交，未推送、未发布；香港生产未操作。下一步唯一事项是用户在开发环境复核受影响流程并明确是否发布。
