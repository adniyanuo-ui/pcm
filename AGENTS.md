# PCM 项目开发约定

开始工作前完整阅读 `项目开发共识.md` 和 `云端开发交接.md`。

## 产品边界

- 产品是给年轻中医大夫使用的医疗机构内临床决策支持工具，不面向普通消费者。
- 主线固定为：语音记录 → 四诊合参 → 辨证抓病机 → 确立治法 → RAG 选基础方 → 大夫增减 → 病历草稿 → 医师确认。
- 大模型只负责整理、比较、解释和起草；方剂事实依据来自 RAG，大夫拥有最终决定权。
- 不展示或依赖模型内部思维链，向医生展示的是四诊、病机、治法和辞典原文构成的证据链。
- 当前忽略预问诊问卷和焦虑/抑郁评分；星级评价与医生风格适配保留为第四模块，暂不开发。

## 开发规则

- 日常开发使用 `develop` 或功能分支；只有经过用户完整试用确认的版本才能合并到 `main`。
- `main` 推送后会自动部署到香港生产实例，避免把实验提交直接推送到 `main`。
- 不把任何 API Key、患者隐私、生产数据库、Codex认证文件或SSH私钥提交到Git。
- 新加坡开发环境不得复制生产患者数据；测试使用虚构或脱敏病例。
- 修改四诊等上游数据时必须使下游辨证、候选方、处方和病历结果失效并重新生成。
- 候选基础方必须带册数、PDF页码/书内页码和辞典原文依据。

## 常用验证

```bash
cd /srv/pcm-workspace/pcm

# 后端
./pcm-master_hyd/.venv/bin/python pcm-master_hyd/manage.py test llm user speech pcm

# 新医生工作台
npm --prefix pcm_doctor_web test
npm --prefix pcm_doctor_web run build

# 检查修改
git diff --check
```

旧 Vue 2 CMS 存在历史依赖问题，不把它的基线构建失败误判为新医生工作台回归。
